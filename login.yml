--- 
- 
  name: "Login into vCenter"
  register: login
  uri:  url: "https://{{ vcenter_server }}/rest/com/vmware/cis/session"
    force_basic_auth: true
    method: POST
    password: "{{ vcenter_pass }}"
    user: "{{ vcenter_user }}"
    validate_certs: "{{ validate_certs }}"


